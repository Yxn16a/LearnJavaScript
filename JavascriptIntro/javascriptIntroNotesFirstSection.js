// we use let for basic syntax
//  let year = 1996
// let is used to declared a variable
// update variables
// score+=5; to update 
//  scores -=1; 
// scores--; 
// scores++; 
// constant= const means that this will not change over time
// you can not update the variable of const
// var runDistance = 26, do not use var so often
// boolean are used to store true or false variables
// best practices do not start with the digit the identifier
// naviming variables: each new word start with upper case letters
// the first case has to be small and others have to be upper 
// name variables name that is boolean has to start with is

// String:
// they must be wrapped in quotes 
// ex let userName = "Yves" or singgle quotes
// remember the quotes around the text
// if you want the quotes
// string are indexed and we start to count them from zero
// example let animal ="Dumbo Octopus"; 
 // animal[0] gives you D
 // string  has a length and that counts from zero
 //we can concantanate string
 // "lol" + ";lol"
 // you can not change string
 // you can not update a character in a string or but you can overwrite it
 // when you add a number on the string
 // 1+ "hi" ="1hi" java script make the number into a string bcse it is difficult
 // to make a string a number
 // String Methods:
 // -strind.method();
 // -length is a property not a mthode
 //- non destructive methods are non destructive methode which means that
 // it makes  a copy
 // tream(): removes spaces at the beginning of the string, it keeps space in the
 // middle. it only works
 // - you can chain them string.trim().toUpperCase();
 // - some methodes accept the
//  -indexOf("cat") find where this string is located, it return the location 
// of the string  and returns the location of the first string 
// string.srice(4) slice from form up to the end the end is not included 
// if the slice(-3) gives the last three string or chalager
// replace('h','H') replace 'h' and 'H'
// template literals: 
// use back tick and then put the expression between then and they will be 
// evaluated
// example: 
//`you bought ${price*quantity} eggs
//`you bought $${price*quantity} eggs and this will be $price*quantity
// NULL value: 
// unidefined is also a primitive value 
// Math: read about them at the mdn 
// Math:
// Math.floor(23.4444) gives 23
//Math.random(0-.99999999) not one included gives 
// a number between one and fiver
// step1= math.random()
//  math.pow(2,5); pow 2 by five 
 // operators:double equals checks if the equals  are equal no matter the
 // types
 // === triple equals it cares about type
 // != doesn not care about type
 //!== cares for the type
//  always to use triple equals and  !==
 
// Console.
//prompt("enter a number") as user inputs
//parseInt(string) turns string into a number